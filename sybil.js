const config = require('./config');

require('./database');

// const url = 'https://api.zksync.io/api/v0.1/v0.1/status';


// const { data } = await Axios.get(url).catch((e) => {
//   throw new Error(`Request to ${e.config.url} failed with status code ${e.response.status}`);
// });

// console.log(data);
